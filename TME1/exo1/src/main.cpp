#include "List.h"
#include <string>
#include <iostream>
#include <cstring>

/*int main () {

	std::string abc = "abc";
	char * str = new char [3];
	str[0] = 'a';
	str[1] = 'b';
	str[2] = 'c';
	size_t i = 0;

	if (! strcmp (str, abc.c_str())) {
		std::cout << "Equal !"<<std::endl;//faute pas de endl flush
	}

	pr::List list;
	list.push_front(abc);
	list.push_front(abc);


	std::cout << "Liste : " << list << std::endl;
	std::cout << "Taille : " << list.size() << std::endl;

	// Affiche à l'envers
	for (i= list.size() - 1 ; i > 0 ; --i) {
		std::cout << "elt " << i << ": " << list[i] << std::endl;
	}
	//Faute pas besoin de supprime les element du tableau car par alloué
	// et la chaine elle meme
	delete[] str;//Faute manque le [] car s'est un tableau

	//test 1.4



}*/
